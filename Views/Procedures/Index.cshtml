@model IEnumerable<SD1_sk072215_MIS4200.Models.Procedure>

@{
    ViewBag.Title = "Procedures";
}

<h2>Prodcedure Information</h2>

<p>
    @Html.ActionLink("Add Procedure", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.procedureCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.doctorID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.procedureCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.doctorID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.procedureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.procedureID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.procedureID })
        </td>
    </tr>
}

</table>
